"use client"
import Home from './home.js'

/*quote not displaying, limit the size of the dog picture to that it is smaller than the window size
figure out account and register pages*/

export default function Page() {
  return(
  <div> 
    <Home />
  </div>
  )
}